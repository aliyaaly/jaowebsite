/*You can also place this code in a separate file and link to it like epoch_classes.js*/
	var recieve_date;     
$(function () {
    //if (document.getElementById('recieve_date') != null) recieve_date = new Epoch('epoch_basic','popup',document.getElementById('recieve_date'));  
});